<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eventogo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventoGo">

        <!-- Login Activity -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.EventoGo" />

        <activity
            android:name=".ui.login.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.clientes.RegistroClienteActivity"
            android:exported="false" />

        <activity
            android:name=".ui.clientes.RegistroProveedorActivity"
            android:exported="false" />

        <activity
            android:name=".ui.clientes.RegistroEventoActivity"
            android:exported="false" />

        <activity
            android:name=".ui.clientes.RegistroPagoActivity"
            android:exported="false" />

    </application>

</manifest>